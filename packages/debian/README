Debian 12.04 package of Kerkerkruip
===================================

Dependencies for building the package:

	sudo apt-get install debhelper devscripts

Copy Kerkerkruip.gblorb to:

	kerkerkruip/Kerkerkruip.gblorb

To build:

	cd kerkerkruip
	debuild -us -uc

Lintian may generate a bunch of warnings at the end, but they can be ignored.
